
# Ready Player Me 3D avatars. Create a free application at studio.readyplayer.me
# and paste its subdomain here (just the subdomain, e.g. "autinerary").
# Leave BLANK to keep the lightweight vector avatar picker — the app works
# either way; nothing 3D renders until this is set.
NEXT_PUBLIC_RPM_SUBDOMAIN=
